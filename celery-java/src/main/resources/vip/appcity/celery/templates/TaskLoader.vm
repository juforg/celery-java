package ${packageName};

import org.kohsuke.MetaInfServices;
import vip.appcity.celery.CeleryTaskLoader;

@MetaInfServices
public class ${taskName}Loader implements CeleryTaskLoader<${packageName}.${taskName}> {
    @Override
    public ${packageName}.${taskName} loadTask() {
        return new ${packageName}.${taskName}();
    }
}
